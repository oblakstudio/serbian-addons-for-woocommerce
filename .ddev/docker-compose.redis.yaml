#ddev-generated
volumes:
  redis:
    name: ddev-${DDEV_SITENAME}-redis
    labels:
      com.ddev.site-name: ${DDEV_SITENAME}

services:
  redis:
    container_name: ddev-${DDEV_SITENAME}-redis
    hostname: ddev-${DDEV_SITENAME}-minio
    image: redis:8.10-alpine@sha256:bd999b5cfee25fb24b8320a31fddbd69f462df44c8138c66e369582937beebc0
    command: /etc/redis/conf/redis.conf
    volumes:
      - ".:/mnt/ddev_config"
      - "ddev-global-cache:/mnt/ddev-global-cache"
      - "./redis:/etc/redis/conf"
      - "redis:/data"
    expose:
      - 6379
    networks:
      - default
    deploy:
      resources:
        limits:
          cpus: "2.5"
          memory: "768M"
        reservations:
          cpus: "1.5"
          memory: "512M"
    restart: "no"
    labels:
      com.ddev.site-name: ${DDEV_SITENAME}
      com.ddev.approot: $DDEV_APPROOT
